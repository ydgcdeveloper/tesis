{% include 'default/index.html.twig' %}

{% block container %}
    <div class="container">
        <h4>Nuevo Estudiante Nacional</h4>
        {% if (status != null) %}
            <h5>{{ status }}</h5>
        {% endif %}
        {{ form_start(form_nacs, {'action':'', 'method':'POST'}) }}
        {{ form_end(form_nacs) }}
    </div>
{% endblock %}