{% include 'default/index.html.twig' %}

{% block container %}
    <div class="container">
        <h4>Nuevo Tipo de Estudiante</h4>
        {% if (status != null) %}
            <h5>{{ status }}</h5>
        {% endif %}
        {{ form_start(form_tipo, {'action':'', 'method':'POST'}) }}
        {{ form_end(form_tipo) }}
    </div>
{% endblock %}